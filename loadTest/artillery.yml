config:
  target: 'http://localhost:3000'
  phases:
    - duration: 300
      arrivalRate: 100
  processor: "./worker.js"
scenarios:
 -name: "hit it one time"
  flow:
    - loop:
      - get:
          url: "/{{listing}}/"
      over:
        -235
        -52
      count: 2