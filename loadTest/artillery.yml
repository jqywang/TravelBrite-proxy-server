config:
  target: 'http://localhost:3000'
  phases:
    - duration: 30
      arrivalRate: 800
  processor: "./worker.js"

scenarios:
  - name: "something"
    flow:
      - function: "generateUrl"
      - get:
          url: "/{{listing}}"
