config:
  target: 'http://localhost:3000'
  phases:
    - duration: 30
      arrivalRate: 100
  processor: "./worker.js"
scenarios:
  - flow:
    - get:
      url: "/{{listing}}/"
  - log: "sent a request to {{listing}}"